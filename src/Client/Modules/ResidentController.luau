-- This module previously wrapped manual resident override remotes.
-- Tenant-era builds keep the module as a stub to avoid breaking requires.

local ResidentController = {}

function ResidentController.Init() end

return ResidentController
